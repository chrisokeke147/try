// TODO: read from a build-time env var once we have separate staging/prod
// dashboard deployments — hardcoded for now since there's only one backend.
export const API_BASE_URL = 'https://api.tryride.ng';
